document.querySelectorAll("[data-product-nav-toggle]").forEach((button)=>{button.addEventListener("click",()=>{const nav=document.getElementById(button.getAttribute("aria-controls"));const open=button.getAttribute("aria-expanded")==="true";button.setAttribute("aria-expanded",String(!open));nav.classList.toggle("open",!open)})});
const params=new URLSearchParams(window.location.search);document.querySelectorAll("[data-query-field]").forEach((field)=>{const value=params.get(field.dataset.queryField);if(value)field.value=value});
document.querySelectorAll("[data-scope-form]").forEach((form)=>{form.addEventListener("submit",(event)=>{event.preventDefault();if(!form.reportValidity())return;const fields=[...new FormData(form).entries()].filter(([,value])=>String(value).trim()).map(([key,value])=>`${key.replaceAll("_"," ").toUpperCase()}: ${value}`);const subject=form.dataset.subject||"StructEvidence scope request";const mail=`mailto:john.success1688@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(fields.join("\n\n"))}`;const status=form.querySelector("[data-form-status]");status.textContent="Scope prepared. Your email application will open; review the message before sending.";window.location.href=mail})});
