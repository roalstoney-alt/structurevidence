{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "authorization": {
      "enum": [
        "ALLOW_PUBLICATION",
        "ALLOW_WITH_LIMITATIONS",
        "ALLOW_PAID_DELIVERY",
        "REFRESH_REQUIRED",
        "HUMAN_REVIEW_REQUIRED",
        "ABSTAIN",
        "VETO"
      ]
    }
  },
  "required": [
    "authorization_id",
    "research_id",
    "report_id",
    "report_version",
    "gdr_se_version",
    "evaluation_mode",
    "evaluation_id",
    "authorization",
    "gate_results",
    "limitations",
    "counterfactual",
    "created_at",
    "valid_until",
    "supersedes_authorization_id",
    "input_bundle_sha256",
    "config_hashes"
  ],
  "title": "authorization_record",
  "type": "object"
}
