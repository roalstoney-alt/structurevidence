# DNS Setup

DNS_CHANGED = NO

Codex did not modify DNS records.

Domain target: `structurevidence.org`

GitHub Pages state must be checked in repository settings after publication. Do not assume HTTPS is active until GitHub confirms certificate issuance.

Allowed current state for this publication package:

CUSTOM_DOMAIN = PENDING_DNS

HTTPS = PENDING_CERTIFICATE
