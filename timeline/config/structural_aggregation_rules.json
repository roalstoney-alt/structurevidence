{
  "aggregation_version": "TIMELINE_AGGREGATION_v0.1",
  "replaced_by": [
    "level_aggregation_rules.json",
    "delta_aggregation_rules.json"
  ],
  "rules": {
    "R1_1": "Structural runtime uses level_state and delta_state, not legacy_structural_effect."
  }
}
